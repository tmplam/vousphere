class Event {
  String id;
  String name;
  String image;
  DateTime day;
  bool isFavorite;
  int numberOfPeople;
  String address;
  Event(this.id, this.name, this.image, this.day, this.isFavorite, this.numberOfPeople, this.address);
}