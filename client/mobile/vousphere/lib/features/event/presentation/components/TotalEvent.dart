import 'package:flutter/material.dart';

class TotalVoucher extends StatelessWidget {
  const TotalVoucher({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
